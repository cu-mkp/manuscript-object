language: python
python:
  - "3.7"
# command to install dependencies
install:
  - rm Pipefile.lock
  - pip install --system
  - pipenv sync
  - virtualenv env
  - source env/bin/activate
  - pipenv install
  - python check_update.py
# command to run tests
# script: